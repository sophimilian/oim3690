# Week 9 - [JavaScript Basics: variables, types and functions]

<h4 class="date"><em>Date: March 24rd, 2026 & March 26th, 2026 </em></h4> 

## Class Notes 
- What did we find in the TODO app?
    - Where is the script tag in my todo.html?
        - It is near the end. The opening tag is in line 271 and the closing one is in 432, with having 434 lines in total (Remember that script is the last part of the body).
    - How does javascript find html elements on the page?
        - JavaScript uses document.getElementById() to search the page for any element that has a matching id. It is like every HTML element having a nametag while JavaScript reads the nametag to find exactly what it is looking for. Once it finds the elemtn, it saves it in a variable so it can be able to use it later.
    - What makes the "add" button actually do something?
        - When you click it, it triggers the "submit" action. The code is listening for that action to happen. When it does happen, it reads whatever text you typed into the input box, stores it as a new todo object (with id, text, category, and completed/starred status both set to false), and it then pushes it into the todos list. Then it saves the updated array and redoes the list. 
    - Where does the code change the page (Add text, add items, etc...)?
        - This happens inside a function called renderTodos(). Every time something changes — adding, deleting, completing, or starring a task — the code wipes everything clean and makes it empty. Then it will loop through all the todos and build brand new HTML elements from scratch using document.createElement(), fills them with text and buttons, and drops them onto the page using appendChild(). So the page never truly "updates", it just gets  redrawn every time. 
- Events are actions that happen on the actual page:
    - user clicks a button
    - user types in an input
    - user presses enter
- once you have an event, this is the core pattern:
1. Find the element
const button = document.querySelector('button');
2. Listen for an event
button.addEventListener('click', function() {} 
) 

- JavaScript only runs on broswer 
    - V8 is google's open-source high-performance JavaScript and WebAssembly engine
- document.querySelector('myButton) returns null, but <button id=""myButton> is right there in the HTML. What is wrong?
    - you need the # before myButton 
- You changed a heading's color in the Console and it worked. After refreshing the page, it is gone. Why?
    - local storage is how a website stores the data, you are not actually changing any files you are just temporarily changing it. 
- for declaring variables, you use let to values that change, const for values that stay the same. 
DATA TYPES
- we will use strongs, numbers, and booleans most of the time. 
- you can use typeof to check what type a value is (arrays return "object")
- ARITHMETIC OPERATORS: standard math works as it usually does. % (modulo) gives the remainder, ** is for exponents 
- STRING OPERATIONS: have built-in methods for common transformations
- COMPARISON OPERATORS: you always need to use === (strict equality) and !== (strict comparison) in JS to avoid any unexpected type conversions.


## Challenges I faced 
- writing my own code in the s16-demo, i had multiple errors so i was just struggling doing that portion myself

## AI usage (if any) 
- asked it to help me remove any js from my todo.html and double check i did everything correctly 
- asked it to remove any additional css that i did not remove and tell me what i missed so my todo was the same exact way that i did it
- help fix any errors i had in s16-demo 
    - after adding my own code to the s16-demo, i asked copilot to add a section where once you click the button, what you typed would be posted as a note; once you refresh the page, everything deletes 

- asked AI to add onto my greeting html code:
    - add a dropdown so the user can pick a language (english, spanish or chinese)
    - add styling so the greeting appears with a nice animation
    - now show a different background color depending on the language selected, but only choose shades of blue

 ## Questions for next time  
N/A