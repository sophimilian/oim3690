# Week 10 

<h4 class="date"><em>Date: March 31rd, 2026 & April 2nd, 2026 </em></h4> 

## Class Notes 
- JS basics: vairables (let/const), data types, operators, and template literals 
REVIEW QUESTIONS:
- you wrote const total = price + tax and got "1005" instead of 15. What happened, and how do you fix it?
    - you need to debug; it is all about the type of inputs that you put --> string is the most important part of this
- Your code has const counter = 0 and later counter = counter + 1. It crashes. Why, and what's the one-word fix?
    - let --> it needs to say const counter = 0 and then let c = 0 and c+= 1
    - if you expect the variable to change, you need to use let 
- Rewrite this using a template literal: "Hello, " + name + "! You have " + count + " items."
    - you need to save the file and then the template will work
- Why does 5 == "5" return true but 5 === "5" returns false?
    - comparison operators: double = is just checking the value and triple = is strict equality (checking the types and value are the same; so they arent the same because the types are not the same)

STRING OPERATIONS
- strings have built-in methods for common transformations:
let first = "Hello";
let last = "World";

// Concatenation
first + " " + last  // "Hello World"

// Length
first.length  // 5

// Methods
first.toUpperCase()  // "HELLO"
first.toLowerCase()  // "hello"
first.includes("ell")  // true
first.startsWith("He") // true 
- logical operators: combine conditions  with && (both must be true), || (either can be true), ! (reverse).
- a resuable block of code that performs a tasks:
// Define function 
function greet() {
  console.log("Hello!");
} 
- arrow functions: shorthand. AI-generate code uses them often, so we need to learn how to recognize them. 


- We have learned JS fundamentals: variables, types, operators, template literals, functions, and conditionals
REVIEW QUESTIONS:
- You wrote function getGrade(score) but calling getGrade(85) returns undefined. What's most likely missing inside the function?
    -  a return statement is missing inside the function, like returning the grade value like "B".
- Your function checks if (score >= 60) first, then else if (score >= 90). What would a student with 95 get? Why?
    - they would get a pass because the code is not well designed and thoughout. it goes through everything sequentially.
- What does this arrow function do? const f = x => x ** 2;
    - it is saying x^2 
- You wrote let username = prompt("Enter your name"); and the user clicked OK without typing anything. Does if (username) run?
    - prompt is a browser API, you can add something so it greets the user; add an if else. If they do not enter a username, it will automatically put them as guest. 
- AND &&: both conditions must be true and it is useful for checking multiple requirements at once. 
- OR ||: at least one condition must be true, useful when any of several conditions will work. 
- Ternary operator: shorthand for simple if/else, used only for brief assingments, not complex logic
- Form validation: it checks the code and makes sure it meets the criteria. Example is --> checks that emial exists and password meets minimum length before allowing submission. 
- Check user input: gives real-time visual feedback at the user types. 
JAVASCRIPT ARRAY & LOOPS:
    - ARRAYS: creating, modifying, searching; it stores multiples values in a single variable
    - push () adds to the end (Most common). unshift () adds to the start
    - pop () removes from the end, shift () from the start. Use splice () to remove by index
    - includes () returns t/f. indexOf() returns the position (or -1 if not found).
    - array vs string 
        - strings are immutable and cannot be changed
        arrays are mutable and can be changed
    - LOOPS: for, while, common patterns
- searching array demo



## Challenges I faced 
- 

## AI usage (if any) 
- 

 ## Questions for next time  
N/A